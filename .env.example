# Copy this file to .env.local and fill in real values.
# .env.local is git-ignored (matches the *.local pattern in .gitignore).

# Supabase project "Capabilio Recruiter" (xogmrwpnkmdmjvwpmfzc)
VITE_SUPABASE_URL=https://xogmrwpnkmdmjvwpmfzc.supabase.co
VITE_SUPABASE_ANON_KEY=

# Backend API (capabilioAI-prog/capabilio-recruiter-backend on Render)
VITE_BACKEND_URL=https://capabilio-recruiter.onrender.com/api

# 2026-08-10: a SEPARATE legacy AI microservice, not this backend -- it has
# no route overlap with capabilio-recruiter-backend (no
# /recruiter/generate-challenge, /recruiter/shadow-interview,
# /recruiter/team-chemistry, or /recruiter/candidate-analysis route exists
# there). Several pages (HiringArena, ShadowInterview, CandidateInterview,
# TeamChemistry, CandidateProfile, TalentIncubator) call it directly for AI
# generation/analysis features that haven't been migrated into the main
# backend yet. Previously hardcoded as a literal in each file; now
# configurable, defaulting to the same URL that was already in use.
VITE_LEGACY_AI_URL=https://capabilio-backend-production-60ab.up.railway.app/api
